---
phase: 09.1-penboard-workspace-external-mermaid-docs
plan: 01
subsystem: mcp
tags: [mcp, workspace, mermaid, flow, markdown, nitro, api]

# Dependency graph
requires:
  - phase: 09-mcp-integration
    provides: MCP tool registry, document-manager, tool-registry pattern
provides:
  - 5 MCP workspace tools (write_flow, read_flow, list_flows, write_doc, read_doc)
  - PenDocument.workspace field for workspace root tracking
  - FlowFile shared type for server/client communication
  - GET /api/workspace/flows endpoint for browser flow tab
  - GET /api/workspace/root endpoint for workspace resolution
  - filePath plumbing through document sync pipeline
  - getCachedFilePath helper in document-manager
affects: [09.1-02, mcp, workspace, flow-tab]

# Tech tracking
tech-stack:
  added: []
  patterns: [workspace-directory-convention, manifest-auto-update, filePath-sync-plumbing]

key-files:
  created:
    - src/mcp/tools/workspace.ts
    - src/mcp/tools/__tests__/workspace.test.ts
    - server/api/workspace/root.get.ts
    - server/api/workspace/flows.get.ts
  modified:
    - src/types/pen.ts
    - src/mcp/tool-registry.ts
    - src/mcp/document-manager.ts
    - server/utils/mcp-sync-state.ts
    - server/api/mcp/document.post.ts
    - src/hooks/use-mcp-sync.ts

key-decisions:
  - "Workspace root = dirname of .pb file (file-based mode); live canvas uses server /api/workspace/root"
  - "Manifest auto-updates on every write_flow/write_doc (upsert pattern)"
  - "filePath plumbing: use-mcp-sync sends filePath -> document.post stores via setSyncFilePath -> workspace/root.get returns dirname"
  - "Tool-registry stays under 800 LOC (788 after adding 5 workspace tool cases)"

patterns-established:
  - "Workspace .penboard/ directory convention: flows/ for mermaid diagrams, docs/{category}/ for context documents"
  - "manifest.json auto-update pattern for tracking workspace files"
  - "Server filePath sync: renderer pushes filePath to server state for MCP workspace resolution"

requirements-completed: []

# Metrics
duration: 6min
completed: 2026-03-20
---

# Phase 09.1 Plan 01: Workspace Backend Summary

**5 MCP workspace tools for reading/writing flow and doc files in .penboard/ directory, plus Nitro server endpoints for browser access and filePath sync plumbing**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-20T20:23:39Z
- **Completed:** 2026-03-20T20:29:48Z
- **Tasks:** 3
- **Files modified:** 10

## Accomplishments
- Implemented 5 MCP workspace tools: write_flow, read_flow, list_flows, write_doc, read_doc with manifest auto-update
- Created server endpoints GET /api/workspace/flows and GET /api/workspace/root for browser-side access
- Plumbed filePath through the document sync pipeline (use-mcp-sync -> document.post -> mcp-sync-state)
- Added PenDocument.workspace field and FlowFile shared type
- All 13 unit tests passing covering write/read/list for flows and docs, manifest updates, and workspace path resolution

## Task Commits

Each task was committed atomically:

1. **Task 1: PenDocument type extension + shared FlowFile type + workspace MCP tools** - `e2637dc` (feat)
2. **Task 2: Server API endpoints + MCP sync filePath plumbing** - `0df9610` (feat)
3. **Task 3: Unit tests for workspace MCP tools** - `09d8d7f` (test)

## Files Created/Modified
- `src/types/pen.ts` - Added workspace?: string field to PenDocument, FlowFile shared interface
- `src/mcp/tools/workspace.ts` - 5 MCP workspace tool handlers + tool definitions + workspace path resolution
- `src/mcp/tool-registry.ts` - Registered WORKSPACE_TOOLS + 5 switch cases (788 LOC)
- `src/mcp/document-manager.ts` - Added getCachedFilePath helper
- `server/api/workspace/root.get.ts` - GET endpoint returning workspace root directory
- `server/api/workspace/flows.get.ts` - GET endpoint returning all flow files for browser
- `server/utils/mcp-sync-state.ts` - Added getSyncFilePath/setSyncFilePath state tracking
- `server/api/mcp/document.post.ts` - Stores filePath via setSyncFilePath on document push
- `src/hooks/use-mcp-sync.ts` - Sends filePath in document push POST body
- `src/mcp/tools/__tests__/workspace.test.ts` - 13 unit tests for all workspace tools

## Decisions Made
- Workspace root = dirname of .pb file (file-based mode); live canvas uses server /api/workspace/root endpoint
- Manifest auto-updates on every write_flow/write_doc with upsert pattern (no manual manifest management)
- filePath plumbing chain: use-mcp-sync sends filePath -> document.post stores via setSyncFilePath -> workspace/root.get returns dirname
- Tool-registry stays at 788 LOC after adding 5 workspace tool cases (under 800 limit)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Pre-existing Vitest/Bun compatibility issue on Windows: `bun --bun vitest` fails with "File URL path must be an absolute path" for all test files (not just workspace tests). Used `npx vitest` instead, which works correctly. This is an out-of-scope infrastructure issue.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Workspace backend complete, all tools registered and tested
- Plan 02 (Flow tab UI) can now use GET /api/workspace/flows to fetch flow data for rendering
- MCP clients can now write/read flows and docs via the 5 workspace tools

## Self-Check: PASSED

All 10 files verified present. All 3 task commits verified (e2637dc, 0df9610, 09d8d7f).

---
*Phase: 09.1-penboard-workspace-external-mermaid-docs*
*Completed: 2026-03-20*
