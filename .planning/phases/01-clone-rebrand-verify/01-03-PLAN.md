---
phase: 01-clone-rebrand-verify
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - electron/main.ts
  - electron-builder.yml
  - src/utils/file-operations.ts
  - src/mcp/server.ts
  - src/mcp/tools/open-document.ts
  - src/mcp/tools/batch-get.ts
  - src/mcp/tools/design-prompt.ts
  - src/routes/index.tsx
autonomous: false
requirements:
  - CANVAS-01
  - CANVAS-02
  - CANVAS-03
  - CANVAS-04
  - CANVAS-05
  - CANVAS-06
must_haves:
  truths:
    - "File open dialogs accept .pb, .op, and .pen files"
    - "File save defaults to .pb extension"
    - "Navigating to / redirects to /editor"
    - "Dev server starts on port 3000 with working canvas"
    - "All 11 shape types render correctly on canvas"
    - "Select, move, resize, multi-select, undo/redo, copy/paste all work"
    - "Alignment guides appear when moving shapes near others"
    - "Build completes without errors"
    - "All tests pass"
  artifacts:
    - path: "src/routes/index.tsx"
      provides: "Landing page redirect to /editor"
      contains: "redirect"
    - path: "electron-builder.yml"
      provides: "Dual file association for .pb and .op"
      contains: "ext: pb"
    - path: "src/utils/file-operations.ts"
      provides: "File dialog with .pb support"
      contains: ".pb"
  key_links:
    - from: "src/routes/index.tsx"
      to: "/editor"
      via: "TanStack Router redirect in beforeLoad"
      pattern: "redirect.*editor"
    - from: "electron/main.ts"
      to: "electron-builder.yml"
      via: "file association + dialog filters"
      pattern: "\\.pb"
    - from: "src/utils/file-operations.ts"
      to: "electron/main.ts"
      via: "file extension validation"
      pattern: "\\.pb.*\\.op"
---

<objective>
Add .pb file extension support, implement landing page redirect to /editor, verify the complete build pipeline, verify all canvas features work, and prepare for fresh git init.

Purpose: This is the integration and verification plan. After Plans 01 and 02 complete the rebrand, this plan adds the new .pb file format support, sets up the redirect, verifies everything works end-to-end, and creates the clean git starting point.

Output: .pb file extension supported everywhere, landing page redirects to /editor, build verified, canvas features verified, ready for development.
</objective>

<execution_context>
@C:/Users/Lionel Messi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lionel Messi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-clone-rebrand-verify/01-CONTEXT.md
@.planning/phases/01-clone-rebrand-verify/01-RESEARCH.md
@.planning/phases/01-clone-rebrand-verify/01-01-SUMMARY.md
@.planning/phases/01-clone-rebrand-verify/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .pb file extension support and landing page redirect</name>
  <files>
    electron/main.ts, electron-builder.yml, src/utils/file-operations.ts,
    src/mcp/server.ts, src/mcp/tools/open-document.ts, src/mcp/tools/batch-get.ts,
    src/mcp/tools/design-prompt.ts, src/routes/index.tsx
  </files>
  <action>
**Layer 7 from RESEARCH.md — File Extension Changes (.pb support):**

Per CONTEXT.md locked decisions:
- Support both `.pb` and `.op` for opening files
- Default save format: `.pb` (PenBoard native)
- File dialog filters show: "PenBoard Files (*.pb, *.op)"
- Electron file association registers both extensions

1. **`electron/main.ts`** — 5 code paths (see Pattern 2 in RESEARCH.md):
   a. `dialog:openFile` filter: Add `.pb` to the extensions array: `['pb', 'op', 'pen']`
   b. `dialog:saveFile` filter + defaultPath: Default to `.pb`: `extensions: ['pb']`, `defaultPath: 'untitled.pb'`
   c. `dialog:saveToPath` validation: Add `.pb` check: `ext !== '.pb' && ext !== '.op' && ext !== '.pen'`
   d. `file:read` validation: Add `.pb` check alongside `.op` and `.pen`
   e. `getFilePathFromArgs()`: Add `.pb` to CLI argument detection: `ext === '.pb' || ext === '.op' || ext === '.pen'`

2. **`electron-builder.yml`** — Add .pb file association entry (see Example 2 in RESEARCH.md):
   ```yaml
   fileAssociations:
     - ext: pb
       name: PenBoard Document
       description: PenBoard Design File
       mimeType: application/x-penboard
       role: Editor
       icon: build/icon
     - ext: op
       name: PenBoard Document (Legacy)
       description: PenBoard Design File (Legacy Format)
       mimeType: application/x-penboard
       role: Editor
       icon: build/icon
   ```
   **CRITICAL — Pitfall 3:** Do NOT remove the `.op` association. Keep both.

3. **`src/utils/file-operations.ts`** — Web file API:
   a. `saveDocumentAs()`: Change suggestedName to `'untitled.pb'`. Update accept types to include `.pb`: `{ 'application/json': ['.pb', '.op'] }`
   b. `openDocumentFS()`: Add `.pb` to accept types
   c. `openDocument()`: Add `.pb` to accept types

4. **MCP tools** — Update tool descriptions that mention `.op` to also mention `.pb`:
   a. `src/mcp/server.ts`: Update any tool descriptions mentioning .op files
   b. `src/mcp/tools/open-document.ts`: Update description/validation to support .pb
   c. `src/mcp/tools/batch-get.ts`: Update description if it mentions file extensions
   d. `src/mcp/tools/design-prompt.ts`: Update description if it mentions file extensions

**Layer 8 from RESEARCH.md — Landing Page Redirect:**

5. **`src/routes/index.tsx`** — Replace the landing page component with a TanStack Router redirect (see Pattern 1 and Example 1 in RESEARCH.md):
   ```typescript
   import { createFileRoute, redirect } from '@tanstack/react-router'

   export const Route = createFileRoute('/')({
     beforeLoad: () => {
       throw redirect({ to: '/editor', replace: true })
     },
   })
   ```
   Use `replace: true` to prevent the landing page from appearing in browser history.
   Remove the LandingPage component import and the component function — only keep the route with redirect.
  </action>
  <verify>
    <automated>cd /d/Workspace/Messi/Code/PenBoard && grep -n "\.pb" electron/main.ts src/utils/file-operations.ts electron-builder.yml | head -20 && echo "---" && grep -n "redirect" src/routes/index.tsx</automated>
  </verify>
  <done>.pb extension is supported in all 5 Electron code paths, web file API, and electron-builder file associations. Landing page route redirects to /editor. MCP tool descriptions updated.</done>
</task>

<task type="auto">
  <name>Task 2: Full build verification and final grep audit</name>
  <files>N/A — verification only, no files modified</files>
  <action>
Run the full verification suite. No files are modified — this is a verification-only task.

1. **Comprehensive grep audit** — find ALL remaining "openpencil" references:
   ```bash
   grep -rn "openpencil\|OpenPencil" --include="*.ts" --include="*.tsx" --include="*.json" --include="*.yml" --include="*.yaml" --include="*.md" . | grep -v node_modules | grep -v ".planning/" | grep -v ".git/"
   ```
   If ANY results appear (other than acceptable historical comments), fix them before proceeding.

2. **Type check:**
   ```bash
   npx tsc --noEmit
   ```
   Must pass with zero errors.

3. **Test suite:**
   ```bash
   bun --bun run test
   ```
   All existing tests must pass.

4. **Production build:**
   ```bash
   bun --bun run build
   ```
   Must complete without errors.

5. **Dev server smoke test:**
   ```bash
   # Start dev server and verify it responds
   timeout 30 bun --bun run dev &
   sleep 10
   curl -s http://localhost:3000 | head -20
   kill %1 2>/dev/null
   ```
   Must get a response (redirect to /editor is expected).

If any step fails, fix the issue before marking this task complete. Common failure causes:
- Type error from mismatched string literals (theme-preset type, Figma layout mode) → fix the mismatched string
- Test failure from hardcoded "openpencil" in test expectations → update test expectations
- Build failure from import path change → verify no import paths were accidentally modified
  </action>
  <verify>
    <automated>cd /d/Workspace/Messi/Code/PenBoard && npx tsc --noEmit 2>&1 | tail -5 && echo "---TSC_DONE---" && bun --bun run test 2>&1 | tail -10 && echo "---TEST_DONE---" && bun --bun run build 2>&1 | tail -10</automated>
  </verify>
  <done>Type check passes with zero errors. All tests pass. Production build succeeds. Grep audit shows zero remaining "openpencil" references in source files.</done>
</task>

<task type="auto">
  <name>Task 3: Fresh git init with PenBoard identity commit</name>
  <files>N/A — git operations only, no source files modified</files>
  <action>
Per CONTEXT.md locked decisions: "Fresh `git init` after all rebrand changes are complete" and "First commit message: `init: PenBoard v0.1.0 — fork of OpenPencil v0.4.1`".

1. **Remove existing git history:**
   ```bash
   cd /d/Workspace/Messi/Code/PenBoard
   rm -rf .git
   ```

2. **Initialize fresh repository:**
   ```bash
   git init
   git add -A
   git commit -m "init: PenBoard v0.1.0 — fork of OpenPencil v0.4.1"
   ```

3. **Verify:** Check that `git log --oneline` shows exactly one commit with the correct message, and `git status` shows a clean working tree.

**NOTE:** GitHub remote is NOT in Phase 1 scope per CONTEXT.md. Do not add any remote.
  </action>
  <verify>
    <automated>cd /d/Workspace/Messi/Code/PenBoard && git log --oneline | head -1 | grep "init: PenBoard v0.1.0" && git status --porcelain | wc -l</automated>
  </verify>
  <done>Fresh git repo initialized. Single commit "init: PenBoard v0.1.0 — fork of OpenPencil v0.4.1" present. Working tree clean. No remote configured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of canvas features and PenBoard branding</name>
  <files>N/A — verification only, no files modified</files>
  <action>
Human verifies the complete PenBoard rebrand visually. Claude has already:
- Rebranded all source code, configs, i18n, and documentation from OpenPencil to PenBoard
- Added .pb file extension support alongside .op
- Implemented landing page redirect to /editor
- Passed type check, tests, and production build

User should now verify interactively:

**Step 1 — Start dev server:** `bun --bun run dev`, open http://localhost:3000

**Step 2 — Verify redirect:** Navigate to http://localhost:3000/ — should redirect to /editor. Browser tab title should show "PenBoard".

**Step 3 — Verify canvas features (CANVAS-01 through CANVAS-06):**
- CANVAS-01: Create Rectangle, Ellipse, Frame, Text, Line. Zoom with mouse wheel. Pan with Space+drag.
- CANVAS-02: Click to select, drag to move, corner handles to resize, rotation handle.
- CANVAS-03: Drag on empty area for marquee selection. Shift+click for multi-select.
- CANVAS-04: Ctrl+Z to undo, Ctrl+Shift+Z to redo.
- CANVAS-05: Select shape, Ctrl+C, Ctrl+V — pasted shape appears.
- CANVAS-06: Move shape near another — alignment guides appear.

**Step 4 — Verify file operations:** Ctrl+Shift+S (Save As) should default to .pb. Ctrl+O (Open) should show PenBoard Files filter.

**Step 5 — Verify Electron (optional):** `bun run electron:dev` — window title shows "PenBoard".

Resume signal: Type "approved" if all verifications pass, or describe any issues found.
  </action>
  <verify>
    <automated>cd /d/Workspace/Messi/Code/PenBoard && echo "CHECKPOINT: Requires human visual verification. Run 'bun --bun run dev' and test canvas features manually."</automated>
  </verify>
  <done>Human confirms: PenBoard branding visible, landing page redirects, all 6 canvas interaction types work correctly, file dialogs show .pb support.</done>
</task>

</tasks>

<verification>
1. `.pb` file extension accepted in all file dialogs (open and save)
2. Default save extension is `.pb`
3. http://localhost:3000/ redirects to /editor
4. Window/tab title shows "PenBoard"
5. All 11 shape types render on canvas
6. Select, move, resize, multi-select, undo/redo, copy/paste work
7. Alignment guides appear
8. `npx tsc --noEmit` passes
9. `bun --bun run test` passes
10. `bun --bun run build` succeeds
</verification>

<success_criteria>
1. File dialogs support .pb (default save) and .op (backward compatible open)
2. Landing page (/) redirects to /editor
3. Dev server starts and canvas renders shapes
4. All canvas interactions work (CANVAS-01 through CANVAS-06)
5. Type check, tests, and build all pass
6. Human verification confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/01-clone-rebrand-verify/01-03-SUMMARY.md`
</output>
