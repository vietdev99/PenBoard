import { describe, it } from 'vitest'

describe('Preview security', () => {
  it.todo('generated HTML contains CSP meta tag with strict policy')

  it.todo('generated HTML contains no external http:// or https:// URLs in src/href attributes')
})
